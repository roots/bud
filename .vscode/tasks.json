{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile",
      "type": "shell",
      "command": "cd test-project && npm run compile",
      "problemMatcher": "$tsc",
      "dependsOn": "npm: install - test-project"
    }
  ]
}
