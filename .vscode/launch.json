{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach by Process ID",
      "processId": "${command:PickProcess}",
      "request": "attach",
      "skipFiles": ["<node_internals>/**"],
      "type": "pwa-node"
    },
    {
      "command": "cd ./examples/basic && yarn bud build",
      "name": "Run yarn build",
      "request": "launch",
      "type": "node-terminal"
    }
  ]
}
