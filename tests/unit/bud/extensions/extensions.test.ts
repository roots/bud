import {extensions} from '@roots/bud/extensions'

describe('@roots/bud', () => {
  describe('extensions', () => {
    it('is a function`', () => {
      expect(extensions).toBeDefined()
    })
  })
})
