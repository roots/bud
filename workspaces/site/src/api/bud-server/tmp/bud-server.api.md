## API Report File for "@roots/bud-server"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import * as Framework from '@roots/bud-framework';
import { Framework as Framework_2 } from '@roots/bud-framework';
import { FSWatcher } from 'chokidar';

// @public
export class Server extends Framework.Service implements Framework.Server.Interface {
    application: Framework.Server.Application;
    boot(): Promise<void>;
    close(): void;
    // (undocumented)
    compile(): Promise<void>;
    get config(): Framework.Server.Configuration;
    instance: Framework.Server.Instance;
    middleware: Framework.Server.Middleware;
    get port(): string;
    // (undocumented)
    run(): Promise<this>;
    // Warning: (ae-forgotten-export) The symbol "Watcher" needs to be exported by the entry point index.d.ts
    watcher: Watcher;
}

```
