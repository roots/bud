---
id: bud.mode
title: bud.mode
description: Specify if a build is being run in a production or development context.
sidebar_label: bud.mode
slug: /bud.mode/
image: 'https://bud.js.org/img/share/docs/bud.mode__bud.mode.png'
---

Specify if a build is being run in a `production` or `development` context.

[bud.mode](/docs/bud.mode/) will always be either `production` or `development`.

When running bud [using the cli](/guides/bud-cli/):

- [bud build](/guides/bud-cli/build) will always run in `production`.
- [bud dev](/guides/bud-cli/build) will always run in `development`.

## Additional information

[bud.mode](/docs/bud.mode/) correlates with `NODE_ENV` and `BABEL_ENV`.
