---
title: bud.fs.s3.exists
description: Check whether a file exists
sidebar_label: exists
---

Check whether a file is present in the bucket. Returns `true` if file is available.

## Usage

```typescript
const fileExists = await bud.fs.s3.exists(`file.json`)
```
