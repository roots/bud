---
title: '@roots/bud-vue'
description: Use Vue in your Bud projects
sidebar_label: '@roots/bud-vue'
hide_table_of_contents: false
---

import Code from '@theme/CodeBlock'
import {Install} from '@site/src/docs/Install'
import {Sandbox} from '@site/src/components/sandbox'
import Vue2PackageJson from './../../../../../examples/vue-legacy/package.json'

[Vue](https://vuejs.org) support can be added by installing the **@roots/bud-vue** package.

## Installation

<Install packages="@roots/bud-vue" />

## Usage

Once installed, vue should be ready to use in your project. The extension is pre-configured to support Vue 3 single file components (runtime only).

## Configuration

### Including the compiler

If you need to bundle the Vue compiler you can disable runtime-only compilation with the `bud.vue.runtimeOnly` method:

```js
export default async bud => {
  bud.vue.runtimeOnly(false)
}
```

### Using Vue 2

If you are trying to configure `@roots/bud-vue` for use with Vue 2 you will need to install the desired
version of `vue` and a compatible version of the `vue-loader` and `vue-template-compiler` packages.

If a custom version of vue is included in the project package.json the extension will automatically detect it and configure itself accordingly.

This is the package.json we use to run integration tests on vue 2 compatibility:

<Code title="package.json" langage="json">
  {JSON.stringify(Vue2PackageJson, null, 2)}
</Code>

## Example

<Sandbox id="bud-example-vue-3-gf1lkk" title="bud-example-vue-3" />
