---
title: Creating a config file
description: Part 3/7 of the Getting Started guide covers creating a bud.config.js file
sidebar_label: Creating a config file
sidebar_position: 4
image: 'https://bud.js.org/img/share/guides/getting-started__config.png'
---

import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'

So far we've been working without a configuration file which is easy but not very flexible.

In this guide we'll add a **bud.config.js** file to our build so that we have a little more control.

## Create bud.config.js

Create a file titled **bud.config.js** file in the project root.

This file exports a single function that will tell **bud** to generate an html file for our application.

<Tabs
  groupId="lang"
  defaultValue="js"
  values={[
    {label: 'Javascript', value: 'js'},
    {label: 'TypeScript', value: 'ts'},
  ]}>
  <TabItem value="js">

```js title='bud.config.js'
module.exports = async bud => bud.template()
```

  </TabItem>
  <TabItem value="ts">

```ts title='bud.config.ts'
import type {Framework} from '@roots/bud'

export default async function (bud: Framework): void {
  bud.template()
}
```

  </TabItem>
</Tabs>

## Rerun the build

Re-run the build using **bud build**.

You'll see output indicating **index.html** has been generated.

```shell {3} title='$ bud build'
bud

- index.html                         467 bytes
- main.js         minimized          56 bytes
- main.css        minimized          51 bytes

Compiled in 604ms
```
