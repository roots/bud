---
title: Build
sidebar_label: Build
---

import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'
import CodeBlock from '@theme/CodeBlock'

Use the [`bud build` command](/guides/cli/build) to compile our project.

```bash npm2yarn
npx bud build
```

TypeScript users should use the `ts-bud build` command instead.

```bash npm2yarn
npx ts-bud build
```
