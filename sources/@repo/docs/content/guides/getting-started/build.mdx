---
title: Build
sidebar_label: Build
---

import Tabs from '@theme/Tabs'
import TabItem from '@theme/TabItem'
import CodeBlock from '@theme/CodeBlock'

Use the [bud build](/guides/cli/build) command to compile our project.

```bash npm2yarn
npm run bud build
```

:::info TypeScript users

If you have authored your config in TypeScript, you need to use the `ts-bud` command instead of `bud`.

:::
