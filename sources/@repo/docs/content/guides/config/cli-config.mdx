---
title: CLI config
description: Configure your project using the bud CLI
hide_table_of_contents: true
---

import CodeBlock from '@theme/CodeBlock'
import Help from '!!raw-loader!@site/src/components/cli-output/build.help.md'

Many config options can be passed via the CLI. Some config options are exclusive to the CLI.

Pass the `--help` flag to `yarn bud build` to see what all is available:

<CodeBlock title="$ yarn bud build --help">{Help}</CodeBlock>
