import type {NextHandleFunction} from 'connect'
import type {
  Application,
  CookieOptions,
  Errback,
  Express,
  Handler,
  NextFunction,
  Request,
  RequestHandler,
  RequestParamHandler,
} from 'express'
import express from 'express'

export default express
export type {
  Application,
  CookieOptions,
  Errback,
  Express,
  Handler,
  NextFunction,
  Request,
  RequestHandler,
  RequestParamHandler,
  NextHandleFunction,
}
