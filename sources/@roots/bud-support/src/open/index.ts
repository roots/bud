import type {App, AppName, OpenAppOptions, Options} from 'open'
import open from 'open'
import openEditor from 'open-editor'

export {open, openEditor}
export type {App, Options, AppName, OpenAppOptions}
