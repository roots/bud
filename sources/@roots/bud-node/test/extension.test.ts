import {describe, expect, it, test} from 'vitest'

import BudNode from '@roots/bud-node'

describe(`@roots/bud-emotion`, () => {
  it(`should be constructable`, () => {
    expect(BudNode).toBeInstanceOf(Function)
  })

  test.todo(`improve this spec`)
})
