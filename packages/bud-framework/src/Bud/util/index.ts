import {when} from './when'
import {proxy} from './proxy'
import pretty from './pretty'
import format from './format'
import logger from './logger'

export default {pretty, format, logger, when, proxy}
