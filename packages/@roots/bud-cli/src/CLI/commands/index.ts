import {command as build} from './build'
import {command as publish} from './publish/publish'
import {command as publishList} from './publish/list'

export const commands = {build, publish, publishList}
