import {Server as Base} from '@roots/bud-server'

/**
 * Server service
 *
 * @public
 */
export class Server extends Base {
  /**
   * Service ident
   *
   * @internal
   */
  public ident = 'server'
}
