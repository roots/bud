import {
  Express as Application,
  Handler,
  RequestHandler,
} from 'express'

/**
 * Express typings.
 * @see {express}
 */
export namespace Express {
  export {Application, Handler, RequestHandler}
}
