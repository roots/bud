## API Report File for "@roots/filesystem"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts
/// <reference types="node" />

import {Container} from '@roots/container'
import * as fs from 'fs-extra'
import * as path from 'path'
import * as resolve from 'resolve-from'

// @public (undocumented)
export class FileContainer extends Container {
  constructor(baseDir?: string)
  // (undocumented)
  get baseDir(): string
  set baseDir(dir: string)
  ensure(key: string): void
  ensureDir(key: string): void
  exists(key: string): boolean
  // (undocumented)
  get fs(): typeof fs
  has(key: string): boolean
  ls(key?: string): any
  // (undocumented)
  get path(): path.PlatformPath
  read(key: string): string
  readJson(key: string): {
    [key: string]: any
  }
  require(key: string): NodeModule
  // (undocumented)
  get resolve(): typeof resolve
  set(key: string, value: any): this
  setDisk(glob: string[]): this
  write(key: string, content: string): void
  writeJson(key: string, content: string): void
}

// @public
class FileSystem_2 extends Container<FileContainer> {
  // (undocumented)
  get baseDir(): string
  set baseDir(dir: string)
  get<T = FileContainer>(key?: string): T
  make(
    key: string,
    options?: {
      baseDir?: string
      glob?: string[]
    },
  ): FileContainer
  get path(): path.PlatformPath
}
export {FileSystem_2 as FileSystem}
```
