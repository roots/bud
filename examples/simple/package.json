{
  "name": "example-simple",
  "private": true,
  "scripts": {
    "dev": "node bud.config.js --env development",
    "build": "node bud.config.js --env production",
    "test": "jest --verbose"
  },
  "devDependencies": {
    "@roots/bud": "file:../../packages/bud"
  }
}
